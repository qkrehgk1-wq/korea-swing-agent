# 프로젝트 헌장 (기획 하네스)

> 이 문서는 "AI 에이전트 하네스, 언제 걷어내야 하는가"를 다룬 영상(메이커 에반)을 검토하고
> 이 저장소에 적용한 결과물입니다. 영상의 핵심 주장: 모델 성능 부족을 메우려던 장치(프롬프트 체인·
> 누적 규칙 파일·멀티에이전트 오케스트레이션·자체검증 루프)는 최신 모델에서는 대부분 걷어내야 하지만,
> **무엇을·왜 만들지를 고정하는 장치(기획 하네스)는 모델이 아무리 좋아져도 사라지지 않는다** — 그건
> 능력 문제가 아니라 이 프로젝트를 만든 사람 머릿속에만 있는 정보이기 때문. 이 문서가 그 정보를 담는
> 자리입니다. 구조는 영상 그대로: ① 문제 정의 ② 판단 기준과 제약 ③ 의사결정 기록.
>
> (참고: 실제 코드 감사 결과 이 저장소는 "실행 하네스" 쪽은 걷어낼 게 거의 없었습니다 — `agentTeams/`
> 17개 파일 중 16개는 이미 결정론 코드고, LLM 호출 체이닝은 6→1로 이미 통합돼 있었습니다. 비어 있던
> 건 이 문서 하나였습니다.)

## 1. 문제 정의

지휘관 본인은 한국 공무원(안전 분야)으로 본업이 있어 매일 KOSPI·KOSDAQ 수백 종목을 손으로
스크리닝하고, 기술적 지표·수급·뉴스를 교차검증하고, 편향 없이 리스크 사이징까지 계산할 시간이 없다.
이걸 못 하면 감으로 매매하거나, 아예 기회를 못 보고 지나간다. 이 시스템이 매일 자동으로 데이터 검증을
거친 후보와 손실관리 계획을 제시하면, 지휘관은 최종 판단만 본인이 내리면 된다 — 시간은 아끼되 결정권은
넘기지 않는다.

## 2. 판단 기준과 제약

절대 넘으면 안 되는 선(제약):
- **자동 주문 실행 금지.** 이 시스템은 어디까지나 의사결정 지원이며, 실제 매매는 항상 사람이 한다.
- **결정형 언어 금지.** "매수해라" 대신 "유력검토/검토/관찰"만 사용한다. 이유: 유사투자자문업
  법률 리스크 + 지휘관의 공무원 겸직 제약(사용자 헌장). 겸직 판정(2026-07-09)상 회피 대상은
  본인 직속 안전 직무뿐이라 이 프로젝트 자체는 무방하지만, 대외 언어는 여전히 신중해야 한다.
- **한국 시장 전용.** 미국 시장 코드는 shadow-only 연구로만 존재하며 라이브 파이프라인·CI에
  절대 연결하지 않는다(연결 여부는 코드리뷰 때마다 실제로 grep해서 확인할 것 — 문서상 약속만
  믿지 않는다).
- **라이브 기대값이 음수인 동안 리스크 예산은 0%.** 사이징은 항상 실측 기대값(R)에서 도출한다.

판단 기준(A보다 B를 우선하는 이유):
- **백테스트보다 라이브 실측을 상위 신뢰로 둔다** — because 백테스트와 라이브가 서로 다른 전략을
  채점하는 버그가 이 프로젝트 역사상 최소 5차례 반복 발견됐다(목표가 불일치, 유니버스 불일치,
  진입봉 처리 불일치, 거래비용 누락, 보유창 정의 불일치). 이 클래스 버그는 계속 재발하므로,
  코드 리뷰 때마다 "라이브와 백테스트가 정확히 같은 규칙으로 채점되는가"를 1순위로 확인한다.
- **신규 인자 추가보다 기존 인자 검증을 우선한다** — because 2026-06-21 진단에서 "인자를 계속
  쌓았지만 단 하나도 실증되지 않았다"가 드러났다. 새 인자는 반드시 factor-attribution 태깅과
  함께 추가하고, 정산 표본이 부족하면(대략 20건 미만) 승격하지 않는다.
- **자동승격보다 다중 게이트를 우선한다** — because 약한 판정 기준 위에 자동 삭제·자동 승격을
  얹으면 사고가 난다(사용자 본인이 실제로 2회 경험). 전략/유전자 승격은 OOS 표본·평균수익·
  손익비·라이브 기대값 게이트를 전부 통과해야만 한다.
- **승률보다 손익비(R 기대값)를 우선한다** — because 승률 80%짜리도 손익비가 나쁘면 기대값이
  마이너스라는 게 코드로 증명됐다(`expectancy.test.ts`). 모든 적합도·평가는 R 기준으로 한다.
- **파라미터 튜닝보다 구조적 개선, 그리고 항상 측정 후 채택** — because 손절 폭을 좁히거나
  보유기간을 늘리는 직관적 가설을 두 번 연속(21·22차 작업) 세웠다가 실측 스윕으로 정반대
  결과를 확인했다. 파라미터를 만지기 전에는 항상 먼저 스윕하고, 결과를 기록한다.

## 3. 의사결정 기록

이미 검증하고 결정한 것을 다시 원점에서 제안하지 않기 위한 기록. "왜 넣었는지 기억이 안 나면
삭제"(영상의 규칙 파일 정리 원칙)를 이 로그에도 적용한다 — 더 이상 유효하지 않은 항목은 지우거나
갱신한다.

- **(2026-06-16) 미국 주식 완전 제거, 한국 전용 피벗.** 이후 미국 관련 코드는 shadow-only
  연구로만 재도입 허용(2026-08 Alpaca 백테스트 스크립트가 이 경계를 지키는지 실제 확인됨 —
  GitHub Actions 어디에도 연결 안 됨).
- **(2026-06-20) 백테스트/진화 유니버스를 라이브와 강제 일치.** 최초 P0(34종목으로 최적화하는데
  실제 스캔은 200종목 — 진화가 통째로 무효)를 발견·수정. **같은 클래스 버그가 2026-08-03에도
  재발**(스크리너 호출부가 종목 리스트를 `undefined`로 넘겨 숨겨진 기본 30종목으로 폴백). 이
  버그 클래스는 반복 재발하므로 코드리뷰 시 항상 최우선으로 확인한다.
- **(2026-06-21) 약세장에도 침묵하지 않기.** 게이트 미달 후보를 완전히 숨기면 시스템이 정상
  작동 중인지 고장인지 구분할 수 없다. watchOnly/섀도 저널 도입, 단 백테스트·진화 점수에는
  미반영(품질에 영향 없음).
- **(2026-06-21~07-12) 3주 기능 동결 후 실측 검증.** 이 락 자체는 해제됐지만, 재발 방지
  원칙("신규 인자를 검증 없이 계속 쌓지 않는다")은 유지한다.
- **(2026-07-05) 승률 지표를 R 기대값 지표로 전환.** 손익비 나쁜 고승률 게놈을 진화가
  과대평가하는 버그를 실측으로 확인했다.
- **(2026-07-19) 아래 두 파라미터 가설은 이미 실측 스윕으로 기각됐다 — 다시 원점에서
  세우지 말 것:**
  - 손절 폭(`maxRiskPct`)을 좁히면 기대값이 개선될 것이다 → 틀림, 좁히면 악화된다(15%가
    실측 고원의 중심).
  - 보유기간을 늘리면(30일) 거래당 기대값이 계속 좋아질 것이다 → 거래당 수치는 최고지만
    OOS 승률이 5할 밑으로 붕괴 — 상승장 베타를 인샘플에서 주운 것이었다. 15일 유지.
- **(2026-07-19) 거래비용(왕복 0.35%) 미반영이 회전율 높은 설정을 체계적으로 유리하게
  왜곡하고 있었다.** 백테스트·라이브 양쪽에 반영 완료.
- **(2026-07-19) 이익보호 청산 사다리(손익분기 0.6R, 트레일 양보 0.5R)는 MFE/MAE 포렌식
  분석으로 처음 도출했고, 이후 유전 알고리즘과 외부에서 이식된 `billionaireStrategy.ts`
  양쪽에서 독립적으로 같은 값에 수렴했다(3중 교차검증).** 이 값은 이미 세 번 검증됐으므로
  다시 처음부터 재탐색할 필요 없다.
- **(2026-07-19) 진화 자동승격 마진을 척도 무관 상대값으로 전환.** 적합도 척도를 바꿨더니
  고정 마진이 진화를 사실상 영구 동결시키는 부작용을 발견했다. 교훈: 척도를 바꾸면 그
  척도에 걸린 상수를 전부 재점검할 것.
- **(2026-07-19) 챔피언 유전자를 단일 진실 원천으로 통일.** 규칙기반 튜너와 진화가 같은
  오버라이드 파일을 놓고 경쟁해, CI 실패 시 챔피언 값이 조용히 소실될 수 있었다.
- **(2026-08-03) 진화 승격에 "라이브 기대값 ≥ 0" 게이트를 추가.** 라이브가 음수인 상태에서
  진화가 계속 새 챔피언을 승격하면, 검증 안 된 토대 위에 계속 새 집을 짓는 셈이 된다. 라이브가
  양수로 돌아설 때까지 진화는 의도적으로 동결 상태다 — 이건 버그가 아니라 설계다.
- **(2026-08-03) `billionaireStrategy.ts` 등 통합 전략 엔진은 shadow_only, CI/라이브 미연결.**
  자체 승격 게이트(OOS≥20건, OOS 기대값>0.10R, 손익비 하락 없음, 라이브 기대값 비음수 등)를
  아직 통과하지 못했다. 재검토 트리거는 "게이트를 통과했는가"이지 "더 세련돼 보이는가"가 아니다.
- **(2026-08-06) v2 재설계 착수 — 전략층은 전면 교체, 측정자는 보존.** 지휘관 판단:
  "시장 상황도 못 읽고, 하락인지 반등인지 구분 못 하고, 순환섹터를 못 탄다." 실측으로 확인된
  구조적 구멍 3개: ①레짐이 후행 지표 4개로 강세/중립/약세만 판정(급락·반등 구분 불가, 지수
  가격만 보고 **참여 폭을 아예 안 봄**) ②**섹터 차원이 통째로 없음** ③전략이 돌파·눌림목 하나뿐.
  **버릴 것**: 레짐 판정기, 약세 일괄차단, 단일 전략 스크리너. **남길 것(전략이 아니라 자)**:
  R기대값·켈리, 청산 사다리, 거래비용 모델, 저널·채점 정합성, 워크포워드, 시세 정확성 게이트 —
  이걸 지우면 새 설계가 나아졌는지 판단할 근거가 사라진다. 저널 40건 정산은 특히 대체 불가.
- **(2026-08-06) 섹터 순환 엔진 신설(`sectorRotation.ts`).** 네이버 업종 데이터로 73개 섹터를
  구성종목 OHLCV에서 등가중 지수로 재구성 → 상대강도 순위와 회전(RS 개선폭) 탐지. 신규 API 키
  불필요. 라이브 확인: 제약 RS20 +35.6·IT서비스 회전Δ +51.6로 순환이 명확히 읽힘 — 구 시스템은
  이 차원을 전혀 보지 못했다. 한계: 업종 구성은 오늘 스냅샷을 과거에 적용하므로 멤버십 드리프트가
  있다(방향성 편향은 없고 과거 레벨이 흐려질 뿐).
- **(2026-08-06) 시장상태 엔진 신설(`marketState.ts`) — 폭(breadth)이 핵심 신규 입력.**
  6상태(상승추세/조정/급락/반등초기/반등확인/횡보) + 근거 + `riskPosture`. 이미 받는 200종목
  데이터로 참여도를 공짜로 계산한다. **가짜 반등 판별이 설계 목적**: 지수는 튀는데 구성종목이
  안 따라오면 `반등초기`(리스크 25%)로 낮춰 잡고, 폭이 확인되면 `반등확인`(80%). 라이브 대조
  결과 구 엔진은 오늘을 "약세=전량차단"으로, 신 엔진은 "반등확인 80%"로 판정 — 참여 폭 202종목 중
  20일선 위 58.9%·5일 상승 89.6%. 주의: `score`는 후행 항이 섞인 참고값이라 전환기엔 낮게 나온다.
  사이징은 반드시 `riskPosture`를 쓰고, 이는 기대값 기반 예산과 **곱해진다**(실측 기대값이 음수면
  전체가 여전히 0%).
- **(2026-08-05) "약세장에서도 반등 국면이면 진입을 허용하자"는 가설을 실측으로 기각했다.**
  배경: 7월 말 급락 뒤 8/3~8/4 강한 반등이 실제로 있었는데도 레짐이 계속 "약세"로 판정돼
  실픽이 4일째 0건이었다(레짐이 MA20<MA60·20일수익률 같은 후행 지표만 보기 때문 — 급락 후에는
  수 주간 약세에 갇힌다). 그래서 "지수가 MA20을 회복했으면 통과시키자"를 검증했더니 **정반대**였다:
  약세 중 지수>MA20 구간은 −0.175R(15건), 지수>MA20 AND 5일수익률>0은 −0.151R(14건)로,
  오히려 계속 하락 중인 약세 구간(+0.043R, 72건)보다 나빴다. 반등 초입 진입은 가짜 반등에
  당한다는 뜻. **재제안 금지 — 하려면 반드시 재실측부터.**
- **(2026-08-05) 약세 필터의 근거가 바뀌었다(필터는 유지, 이유가 달라짐).** 원래 근거는
  "약세 진입은 평균 손실(승률 52%·−1%)"이었으나, 청산 사다리와 거래비용을 반영해 다시 재면
  약세는 **손실이 아니라 대략 본전**(87건·승률 63.2%·평균 +0.41%·+0.006R)이다. 이익보호
  사다리가 하방을 메운 결과. 다만 강세(98건·66.3%·+4.17%·+0.31R)에는 크게 못 미치므로 필터는
  유지한다 — 이제 목적이 "손실 회피"가 아니라 "위험대비 수익이 나쁜 구간 회피"로 바뀐 것.
  부수 효과로 약세 장기화 시 저널 수집이 사실상 멈추므로(관찰 후보도 20일 쿨다운에 걸림)
  검증 표본 축적이 느려지는 건 감수한 트레이드오프다.
- **(2026-08-03) 스캔 유니버스를 시가총액 상위 200(코스피120+코스닥80)에서 전체 시장(~2200종목)
  으로 확장하는 것을 실측 후 기각했다.** 호출량 자체는 문제 없었다(네이버 단일 소스로 전체
  OHLCV 72초·성공률 99.7%). 하지만 실제 백테스트에서 기존 상위 200 소속 거래는 여전히
  양수(+0.188R, 12건)인데, 새로 편입된 중소형주 거래는 뚜렷한 음수(−0.13R, 156건·고유종목
  146개)였고 합산 기대값이 +0.412R → −0.107R로 뒤집혔다. **시총 상위 캡은 임의의 성능 제약이
  아니라 사실상 유동성·품질 필터였다.** 다시 제안하려면 반드시: ①동일한 실측 백테스트로
  재확인 ②"중소형주 전체"가 아니라 유동성 하한(거래대금·시총 등) 필터를 얹은 부분 확장부터
  시도. 관련 코드 주석: `technicalSwingScreener.ts`의 `resolveSwingUniverse`.

---

*이 문서는 살아있는 문서입니다. 새로운 구조적 판단이 생기면 위 형식으로 추가하고, 더 이상 유효하지
않은 항목(예: shadow 전략이 게이트를 통과해 라이브로 전환된 경우)은 그 자리에서 갱신하세요.*
